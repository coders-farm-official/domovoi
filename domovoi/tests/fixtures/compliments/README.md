# Compliments (test fixture)

The minimal-plugin worked example (design §2.1), used as the install
fixture by `domovoi/tests/test_plugin_installer.py`: one handler in the
general plugin band, one poll worker, one migration, no python
requirements (so the pip steps of the pipeline are exercised as no-ops).
